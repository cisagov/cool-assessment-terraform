# cloud-config
# vim: syntax=yaml
---

write_files:
  - path: /var/lib/cloud/instance/freeipa-creds.sh
    permissions: '0400'
    owner: root:root
    content: |
      ADMIN_PW=${admin_pw}
      HOSTNAME=${hostname}
      REALM=${realm}
